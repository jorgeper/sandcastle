---
"@ai-hero/sandcastle": patch
---

conversational-prd template: the filer's repo grounding is now explicitly lightweight — a budgeted quick survey that locates likely files and searches for duplicate issues, with reproduction and diagnosis forbidden (the implement lane owns debugging, so anything the filer diagnosed was redone there). Code pointers in the rewritten body are best-effort fallout of the survey, never a hunt.
